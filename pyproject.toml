[project]
name = "auth-api"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "pwdlib[argon2]>=0.3.1",
    "pyjwt>=2.13.0",
    "python-decouple>=3.8",
    "sqlmodel>=0.0.39",
]

[tool.fastapi]
entrypoint = "src.app.main:app"
